- if current_user
  %dl.sub-nav
    -# TODO: use columns instead
    -# whitespace
    %dt
    %dd
    %dd
    -# whitespace
    - [["Create Notification", new_notification_path],
      ["Reports", notifications_path],
      ["e911", e911s_path],
      ["GeoMaps", geos_path],
      ].each do |label,path|
      %dd{class: ('active' if (path == request.path.sub(/\/\d+(\/edit)?/,'')) ) }
        = link_to label, path
  %hr
