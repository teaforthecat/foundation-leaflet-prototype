.row
  .small-12.columns
    %h3 New Notification
= render 'form'
= link_to 'Back', notifications_path

%a.button{href: '#', data: {'reveal-id' => 'ensMap', reveal: true}}
  Edit Map
= render 'geos/map_modal',
  modal_id: 'ensMap',
  src: new_geo_path(sse_channel: @notification.sse_channel, iframe: true)
